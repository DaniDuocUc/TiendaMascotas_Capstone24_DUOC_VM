stages:
  - stage: SonarQubeAnalysis
    displayName: SonarQube Analysis Stage
    dependsOn: []
    pool:
      name: 'Azure Pipelines'
    jobs:
      - job: SonarQubeAnalysis
        displayName: SonarQube Analysis
        pool:
          name: 'Azure Pipelines'
        continueOnError: true
        steps:
          - task: SonarQubePrepare@6
            inputs:
              SonarQube: 'sonarqube' # Name of the service connection
              scannerMode: 'CLI'
              configMode: 'manual'
              cliProjectKey: 'Farming_Smart_farming-smart-ws_c172bed6-e5da-4d31-b766-5fce33f21e55' # Project key generated by SonarQube
              cliSources: '.'
          - task: SonarQubeAnalyze@6
          - task: SonarQubePublish@6
            inputs:
              pollingTimeoutSec: '300'
